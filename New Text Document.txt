investigate freq setting f  key setting, a key and o key
pull qihong coded from hardware-interfacing (btw the variable that read from adc(potential) meter in main.c in hw_lab_test is "shared_amp"

it scales from 0 to 1 based on adc reading, log scale, updates every 50ms

it also scales the dummy generate_sine_wave() function in hw.c

references generate_sine_wave func)

misha ux exit comments

after done put all necessary files into one folder(incl mainc) for testing, plus a readme of what to compile and what is expected
(This should include all the necessary files needed for root mainc to compile
recommend a new branch